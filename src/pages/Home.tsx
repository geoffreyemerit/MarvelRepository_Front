import React from 'react';
import FiguresCard from '../components/FiguresCard';

const Home = () => {
  return (
    <div className="home">
      <FiguresCard id={1} />
    </div>
  );
};

export default Home;
